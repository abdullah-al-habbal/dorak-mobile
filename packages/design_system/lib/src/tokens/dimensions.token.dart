import 'package:flutter/material.dart';

class DorakDimensions {
  static const double unit = 8.0;
  static const double gutter = 24.0;
  static const double marginMobile = 20.0;
  static const double marginDesktop = 64.0;
  static const double containerMaxWidth = 1280.0;

  static const BorderRadiusGeometry radiusSm = BorderRadius.all(Radius.circular(4));
  static const BorderRadiusGeometry radiusDefault = BorderRadius.all(Radius.circular(8));
  static const BorderRadiusGeometry radiusMd = BorderRadius.all(Radius.circular(12));
  static const BorderRadiusGeometry radiusLg = BorderRadius.all(Radius.circular(16));
  static const BorderRadiusGeometry radiusXl = BorderRadius.all(Radius.circular(24));
  static const BorderRadiusGeometry radiusFull = BorderRadius.all(Radius.circular(9999));

  static const double spacingUnit = unit;
  static const double spacingSmall = unit;
  static const double spacingMedium = unit * 2;
  static const double spacingLarge = unit * 3;
  static const double spacingXLarge = unit * 4;
}
